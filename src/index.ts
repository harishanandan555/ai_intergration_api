import './LoadEnv'; 
import { intializeDB } from './config/intialdatabase/db';
const config = require('./config/settings');    

(async function() {
    
    // await intializeDB(process.env['NODE_ENV']);

})()
